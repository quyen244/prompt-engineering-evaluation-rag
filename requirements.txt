# File này giữ lại cho tương thích ngược.
# Danh sách thật nằm ở requirements/base.txt (có pin version + ghi chú phase).
#
# Cài đặt đúng cách:
#   py -3.11 -m venv .venv
#   .venv\Scripts\activate
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements/base.txt

-r requirements/base.txt
